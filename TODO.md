# aki-gsub

## Todos

- [ ] multiple match algorithm. like a glob, plain.

+ idea:
  * -e "r:..." regular expression match
  * -e "g:..." glob match
  * -e "s:..." plain string match
+ idea:
  * -e "r:..." regular expression match
  * -e "g:..." glob match
  * -e "s:..." plain string match
